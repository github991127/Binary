# 抽牌概率问题

本项目将抽牌问题拆分为三个子问题，均采用**无放回抽样**，并以默认牌库 `res/Card.xlsx`（$N=160, K=80$）为例进行说明。

## 输入数据

| 参数                    | 数值      | 说明                          |
| ----------------------- | --------- | ----------------------------- |
| 总牌数 $N$              | 160       | 牌堆总量                      |
| 关键牌数 $K$            | 80        | 得分牌数量                    |
| 花色                    | 4 种      | ♠ / ♣ / ♥ / ♦                |
| 点数                    | 1 ~ 13    | 共 13 种点数                  |
| 花色点数组合数          | 最多 52   | $4 \times 13$，实际取决于替换后的牌库 |
| 单组牌数                | 不确定    | 默认牌库多数为 3 张，少数为 4 张 |

> 在 `Card.xlsx` 中：
> - `color` 为数字列（1/2/3/4）
> - `color2` 为花色符号列（♠ / ♣ / ♥ / ♦）
> - `number` 为点数列（1–13）
> - `isTrue = 1` 表示该牌为关键牌

默认牌库中关键牌占比：$K/N = 80/160 = 50\%$。

---

## 问题 1：超几何分布抽取

### 背景

牌堆共 $N$ 张牌，其中 $K$ 张为关键牌。一次性无放回地抽取 $a$ 张牌，每抽到 1 张关键牌得 1 分。需要计算得分 $0$、至少 $1$、至少 $2$、……、至少 $b$ 的概率。

> 问题 1 同时展示 PMF $P(X=k)$ 与“至少 $k$ 分”概率 $P(X \ge k)$，对应 `source/static/js/app.js` 中的概率分布表。输入校验要求 $a \le N$、$b \le a$，且均为非负整数。

### 示例（$N=160, K=80, a=3, b=3$）

| 得分目标 | 概率（精确值） | 概率（近似值） |
| -------- | -------------- | -------------- |
| $P(X=0)$ | $13505/133984$ | $10.08\%$     |
| $P(X\ge 1)$ | $93/106$    | $87.74\%$     |
| $P(X\ge 2)$ | $1/2$       | $50.00\%$     |
| $P(X\ge 3)$ | $13/106$    | $12.26\%$     |

### 通用求解公式

设 $X$ 为抽到的关键牌数量，则 $X$ 服从超几何分布：

$$
P(X = k) = \frac{\binom{K}{k} \cdot \binom{N-K}{a-k}}{\binom{N}{a}}
$$

其中 $k$ 的取值范围为：

$$
\max(0, a-(N-K)) \le k \le \min(a, K)
$$

至少 $t$ 分的概率为：

$$
P(X \ge t) = \sum_{k=t}^{\min(a,K)} P(X = k)
$$

期望值为：

$$
E[X] = a \cdot \frac{K}{N}
$$

---

## 问题 2：花色点数分布（条件概率）

### 背景

将牌库按花色 $s$（`color2`）和点数 $r$（`number`）分为若干组合。对于组合 $(s, r)$，计算在**已知抽到该组合的条件下**，对应牌为关键牌的条件概率 $q_{s,r}$：

$$
q_{s,r} = \frac{\text{该组合中的关键牌数}}{\text{该组合中的总牌数}} = \frac{k_{s,r}}{m_{s,r}}
$$

其中 $m_{s,r}$ 为该组合的总牌数，$k_{s,r}$ 为该组合中的关键牌数。若某组合在牌库中不存在，则概率为 0。

### 结果展示

- 以 $4 \times 13$ 热力图展示各组合的条件概率。
- 概率范围 $[0, 1]$，对应百分比 $[0\%, 100\%]$。
- 支持点击热力图单元格进行**多选 / 取消选择**，下方同步显示已选组合的汇总详情。
- 点击“在牌库中查看”可跳转牌库 Tab，并自动筛选出**所有已选组合**对应的牌张明细。

---

## 问题 3：花色点数抽取（高手 vs 新手）

### 背景

在问题 2 的组合基础上，无放回地抽取 $a$ 张牌。每张牌属于某个花色点数组合，对应条件概率 $q_{s,r}$。玩家从中选择 1 张牌，若为关键牌则得分。

比较两种策略：

| 策略 | 选择方式 | 通用公式 |
| ---- | -------- | -------- |
| 新手 | 无视花色点数，随机选一张抽出的牌 | $P_{\text{新手}} = \dfrac{K}{N}$ |
| 高手 | 选择抽出牌中条件概率 $q_{s,r}$ 最大的一张 | $P_{\text{高手}} = E\left[\max(q_1, q_2, \dots, q_a)\right]$ |

### 示例（$N=160, K=80, a=3$）

| 策略 | 精确概率 | 近似概率 |
| ---- | -------- | -------- |
| 新手 | $1/2$ | $50.00\%$ |
| 高手 | $2635631/4019520$ | $65.57\%$ |

### 高手策略通用公式（无放回）

将所有花色点数组合按 $q_{s,r}$ 升序排列，设 $q^{(1)} < q^{(2)} < \cdots < q^{(m)}$ 为互不相同的概率值。

令 $C_j$ 表示关键概率 $\le q^{(j)}$ 的牌数之和，并设 $C_0 = 0$。则：

$$
P_{\text{高手}} = \sum_{j=1}^{m} q^{(j)} \cdot \frac{\binom{C_j}{a} - \binom{C_{j-1}}{a}}{\binom{N}{a}}
$$

约定当 $C_j < a$ 时，$\binom{C_j}{a} = 0$。

> 直观解释：$\max(q_1, \dots, q_a) = q^{(j)}$ 当且仅当抽出的 $a$ 张牌全部来自概率 $\le q^{(j)}$ 的牌，且至少有一张来自概率恰好为 $q^{(j)}$ 的牌。

---

## 关键说明

1. **为何三个子问题都使用无放回模型？**

   - 问题 1 的“一次性抽 $a$ 张牌”属于**无放回抽样**，因此 $X$ 服从超几何分布，而不是二项分布。
   - 问题 2 计算的是“已知抽到某花色点数组合”的条件概率。
   - 问题 3 的高手策略同样基于无放回抽取 $a$ 张牌后，再选择最优组合。

2. **输入校验规则**

   - 抽牌数 $a$ 必须为非负整数，且 $a \le N$。
   - 最大得分 $b$ 必须为非负整数，且 $b \le a$。
   - 空输入或非整数输入需给出中文错误提示。

3. **数据不可修改**

   加载后的牌库数据在内存中只读，任何计算均基于原数据的深拷贝或只读视图。

4. **与代码的对应关系**

   | 问题 | 主要实现 |
   | ---- | -------- |
   | 问题 1 | `source/Probability.py::problem1_score_distribution` |
   | 问题 2 | `source/Probability.py::problem2_group_probabilities` |
   | 问题 3 | `source/Probability.py::problem3_probabilities` |
   | 热力图多选高亮 | `source/static/js/charts.js::renderProblem2` |
   | 热力图与牌库联动 | `source/static/js/app.js` + `source/static/js/cards.js` |
